<!-- What changes, and why. Link the issue if there is one. -->

- [ ] `cargo test --workspace --locked` passes
- [ ] Anything that can change what gets deleted keeps the item when evidence is missing, and has a test that fails otherwise
- [ ] No API keys, tokens, hostnames or data from a real library in the diff
